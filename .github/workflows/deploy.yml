name: Main Deployment
on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main
jobs:
    unit-test-restapo:
        runs-on: ubuntu-latest
        env:
            PRODUCTION_DATABASE_URL: ${{secrets.POSTGRES_PASSWORD }}
            SECRET: ${{secrets.POSTGRES_PASSWORD }}
            FILEPATH: ${{secrets.POSTGRES_PASSWORD }}
            EMAIL: ${{secrets.POSTGRES_PASSWORD }}
            REFRESH_TOKEN: ${{secrets.POSTGRES_PASSWORD }}
            CLIENT_ID: ${{secrets.POSTGRES_PASSWORD }}
            CLIENT_SECRET: ${{secrets.POSTGRES_PASSWORD }}
            REDIRECT_URI: ${{secrets.POSTGRES_PASSWORD }}

        services:
            postgres:
                image: postgres

                env:
                    POSTGRES_PASSWORD: ${{secrets.POSTGRES_PASSWORD }}
                    POSTGRES_USER: ${{secrets.POSTGRES_USER}}
                    POSTGRES_DB: ${{secrets.POSTGRES_DB}}
                options: >-
                    --health-cmd pg_isready
                    --health-interval 10s
                    --health-timeout 5s
                    --health-retries 5
                ports:
                    - 5432:5432
        strategy:
            matrix:
              node-version: [18.x]
        defaults:
              run:
                  working-directory: restapi
        steps:
            - uses: actions/checkout@v3
            - uses: actions/setup-node@v3
              with:
                node-version: ${{ matrix.node-version }}
            - run: npm ci --legacy-peer-deps
            - run: npm run migrateDb
            - run: npm test


            
    